---
import type { BaseEntry } from "@lib/craft-cms/fetch-content";

// @SYNC
interface Entry extends BaseEntry {
  title: string;
}

const entry: Entry = Astro.props.entry;
---
Example Single

{entry.title}